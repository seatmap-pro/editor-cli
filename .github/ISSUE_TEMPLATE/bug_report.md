---
name: Bug report
about: Something the CLI or the MCP server does wrong
labels: bug
---

## What happened

## What you expected instead

## Steps to reproduce

```bash
# the exact command, with --verbose
```

## Output

```
# paste it here, minus anything you would rather not publish
```

## Environment

- `seatmap --version` or the installed package version:
- Node version (`node --version`):
- Operating system:
- Editor service version and whether it is self-hosted:
- For an MCP issue, which client and whether the server was started with `--allow-write`:
